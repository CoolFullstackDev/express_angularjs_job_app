var config = {
    //url to be used in link generation
    url: '/api',
	
    //server details
    server: {
        host:   '127.0.0.1',
        port:   process.env.PORT || 8000
    }
};

module.exports = config;